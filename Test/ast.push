#prelude(push)

struct A {
    foo:u32,
    bar:u32,
}

struct A {
    foobar:f32
}

trait Add {
    add(self, other)
}

impl Add for A {
    add(self, other) {
        foo + other.foo
    }
}

mod submodule {
    struct B {
        a,b
    }
}

base::B::A::b::a::function (a, b) -> u32 {
    {
        let a:A = {1,2};
    }
    
    fn {
        a+b
    }

    fn()
}
