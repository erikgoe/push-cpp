#prelude(push)

struct A {
    foo:u32,
    bar:u32,
}

struct A {
    foobar:f32
}

struct X::Amb {}
struct Y::Amb {}

base::function (a, b) -> Unknown {
    {
        let a:A = {1,2};
    }
    
    fn {
        a+b
    }

    fn()
}
