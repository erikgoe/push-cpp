#prelude(push)

/// Basic function without anything special
function {
    //let val = 42 + 5 * (3 + 2) + "this is a string";
    //(a, b, c, d, 5, (4%2));
    do {
        function(c, d);
    } until true;
}
