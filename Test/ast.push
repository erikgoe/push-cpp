#prelude(push)

/// Basic function without anything special
function {
    // Anonymous struct
    let a:struct = { name = "john", age=21 };
}

// Global struct
struct A {
    val1:int,
    val2:float
}
